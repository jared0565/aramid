# aramid 1.0 -- blocker task list (master)

> **For agentic workers:** this is a MASTER list, not an implementation plan. The blockers span
> independent subsystems, so each code-bearing task group gets its own plan
> (superpowers:writing-plans -> superpowers:subagent-driven-development) when it is picked up.
> Docs-only tasks carry exact file:line and the source of the correct text, and can be executed
> straight from this list. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** everything that stands between aramid 0.18.0 (`93fec9b`, 2026-09-20) and a 1.0.0 tag that
is honest by the repo's own standard.

**Definition of done (the repo's own, `RELEASING.md` "The 1.0 gate", line 125):**
1. `aramid fleet` reads `ready` -- every registered repo green on every criterion, held >= 14 days
   across >= 2 aramid versions, a repo armed, no disarm inside the streak.
2. API freeze -- the two most recent releases carry no `Changed`/`Removed` entry against the declared
   compatibility surface (CLI names and flags, exit codes, `check --json` keys, ledger statuses,
   `aramid.toml` keys).
3. (added by this list) the surface being frozen is *declared where users read, documented truthfully,
   and pinned by tests* -- today it is none of the three.

**Where things stand, measured 2026-09-21:** gate 1 reads `insufficient-data` (3/7 repos green, 2 stale,
streak 0 d). Gate 2 passes on headings today (0.18.0 = Added/Fixed, 0.17.11 = Fixed) -- but several
tasks below are deliberate `Changed` entries, so the window restarts after they ship. No open
unsuppressed finding, no open GitHub issue or PR, CI green. Of 309 workflow runs since 08-08, two
needed a second attempt: one `aramid.yml` run (09-14, root-caused, fixed `bd37772`) and one
`release.yml` run (08-12, not investigated here).

**Re-measured 2026-09-25 03:50Z [V]** (`aramid fleet`, `~/.aramid/fleet_health.jsonl`, `notices.jsonl`;
verdict `computed_at` 02:00:22Z): still `insufficient-data`, now **4/7 green, 1 stale**. pawscout-worker
turned green under 0.18.0 (rows since 09-22, `consumers_healthy` True); notice `2a0c051b89e1` CLEARED
itself 09-21 10:11Z, reason "defect absent from latest row". graphite green (09-24 pre-push). Still red:
Atlas_Data (20.0 d stale), File Convert, pivot-parlor -- details under DEC-10. **aramid's own newest row
is 2026-09-20 19:39:43Z; under the 7-day window aramid itself turns stale ~2026-09-27 19:40Z** unless a
gate row lands first (pre-commit rows count toward `latest`). Still 0 open unsuppressed, pending-retest 0,
queue empty, HEAD == origin/main == `93fec9b`, CI green, no open issue or PR. Phase 0 unruled.

**Evidence grades used below**
- **[V]** verified first-hand this session (file read or command run by the controller).
- **[A]** reported by a read-only audit agent with file:line; not re-read by the controller. Verify
  before acting (`verify-before-repeating`).
- **[I]** inherited from the session log / memory.

**Priority**
- **BLOCKER** -- required by the 1.0 gate, or 1.0 would freeze a contract that is wrong, undeclared
  or unpinned, or ship a security-relevant falsehood in the docs.
- **SHOULD** -- hardening a production release ought to have; 1.0 is defensible without it if the
  limitation is documented.
- **ACCEPT** -- document as a known limitation; no code.

## Global constraints (every task inherits these)

- TDD at unit scope: pins in `tests/unit/test_<stem>_<aspect>.py` (unique basename; `pytest --co tests`
  before staging). See RED first.
- Per-fix rule: stage-1 sweep against the generator's mutants (`stage1_sweep.py`, naming the UNIT file);
  replicate the CI latent-mutant ratchet locally (expect 0 files over); CHANGELOG entry;
  `aramid check --staged`; commit via `-F` file; quiet push; CI 7/7 by full sha.
- A pin that compares two computations is perturbed by ADDING a member in a scratch copy, never by
  breaking one; a hand-list checked against itself proves nothing.
- Never `--no-verify`. Never `pip install -e .`. Releases only on the operator's word. PyPI approval is
  the operator's. Stay inside this repository; other repos' work goes to their agent via the channel
  (re-list immediately before posting).
- Anything in `src/aramid/` (incl. templates, help strings, docstrings) reaches consumers only through
  release + promote. `README.md` is the PyPI page and updates only at a release.
- A template change (`ARAMID.md`, hook shims) additionally needs every consumer to re-run `aramid init`
  -- announce it on the channel with the release.

---

## Phase 0 -- operator decisions (nothing below them can be frozen until they are made)

Each has a recommendation; none is mine to make.

**RULED by the operator, 2026-09-25 ~04:00Z (05:00 London):** DEC-1..9 -- every recommendation
accepted as written. DEC-10 -- all three red repos (Atlas_Data, File Convert, pivot-parlor) are REMOVED
from the readiness count, via FN-1 built first as a real command (no hand-run registry edit).
pawscout-worker, graphite, demo-store2 and aramid stay. The list itself: commit it.

- [x] **DEC-1 Scope of 1.0.** README:111-117 lists 2c as "in progress" (app auto-start runtime, nuclei
  enrichment, armed-BLOCK for DAST -- all three genuinely absent [V]) and Phases 3/4 as future.
  *Recommend:* declare the three 2c items post-1.0; DAST stays advisory in 1.x.
- [x] **DEC-2 The `--strict` contract.** Code remaps 2 only (`commands/check.py:245` [V]); README:93 and
  knowledge-base:481 say "2 and 3" [V]; user-guide:208 is right. *Recommend:* freeze the code's
  behaviour (3 already fails a CI step), fix the two docs.
- [x] **DEC-3 Legacy top-level `test_command`.** Honoured as a fallback (`pipeline.py:971`,
  `doctor.py:500-509` [A]), pinned by two tests, called "Legacy alias" in the KB, no runtime notice.
  *Recommend:* bless it for 1.x as a documented alias (zero cost, zero breakage); removal is a 2.0 item.
- [x] **DEC-4 Unknown / misplaced / mistyped `aramid.toml` keys** are silently accepted today
  (`config.py` has no known-key set [V]); `tests/unit/test_arm_misplaced_key.py` records this producing
  a falsely "armed" gate [A]. *Recommend:* WARN on stderr + a `doctor` row, never an exit-code change.
- [x] **DEC-5 Versioning of machine-readable output.** Only `fleet --json` and the `~/.aramid` fleet/notice
  files carry `schema_version`; `check --json`, `ledger filter --json`, `ledger consumers --json`,
  `resolvers --json`, `mutation-score --json`, `.aramid/ledger.db`, `repos.toml`,
  `autolearn_state.json` do not [V for reporter/ledger, A for the rest]. *Recommend:* add
  `schema_version` everywhere (additive), `PRAGMA user_version` on the ledger.
- [x] **DEC-6 Exit-code outliers.** `aramid hooks` returns 2 for a REFUSAL
  (`commands/hooks_template.py:90` [V]) where the contract says 3; `schedule status` returns 3 for
  "not installed" [A]; `resolvers` deliberately follows `status`'s contract, undocumented [A].
  *Recommend:* change `hooks` refusal to 3 now (a `Changed` entry, so it must precede the freeze);
  keep and document the other two.
- [x] **DEC-7 What else is "the surface".** `RELEASING.md:130` names five parts. Not named, yet scripted
  against in practice: the 7 MCP tool names/params, `ARAMID_*` environment variables
  (`ARAMID_HOOK`, `ARAMID_ACCEPT_DEGRADED`, `ARAMID_FLEET_DIR`, ...), the `.aramid-suppressions.toml`
  format, the finding FINGERPRINT (README:124 -- a change re-fingerprints accepted findings and can
  escalate them to BLOCK), and "no Python API" (CLI + MCP only). *Recommend:* add all five to the
  declaration; promise fingerprint stability within 1.x or a CHANGELOG-announced `rebaseline`.
- [x] **DEC-8 Dependency caps at 1.0.** `ruff>=0.6,<0.17` (`pyproject.toml:69` [V]) is the cap that
  bites: ruff is pre-1.0 with fast minors, and the in-file rationale says the cap is load-bearing.
  *Recommend:* keep the cap, commit in `RELEASING.md` to a release per ruff minor, and add a
  non-blocking CI canary leg against the newest ruff so the next break is seen before users see it.
- [x] **DEC-9 Security support policy for 1.x.** `SECURITY.md:39` justifies latest-only with "aramid is
  pre-1.0" [V] -- self-invalidating on the day 1.0 ships. *Recommend:* latest 1.x only, restated
  without the pre-1.0 rationale.
- [x] **DEC-10 Fleet membership for the readiness verdict.** Strict readiness grades every REGISTERED
  repo. Atlas_Data (16.0 d stale, mutation stood down, last row under 0.12.0), pivot-parlor (7.5 d
  stale, eslint skip), File Convert (row under 0.17.9) [V from `fleet_health.jsonl`]. Keep and revive,
  or deregister. *Recommend:* decide per repo; deregistration needs FN-1 to be a command, not a
  hand-run Python call.
  *Re-measured 2026-09-25 [V], newest row per repo:* **Atlas_Data** unchanged -- 09-05 02:05Z, 0.12.0,
  20.0 d stale (the nested `atlas_gui` rows are a separate, unregistered path). **File Convert** now has
  6 rows under 0.18.0; newest pre-push 09-21 07:26Z: exit 2, 0 blocking, `skip_streaks.pre-push.eslint`
  = 5, `stood_down: [js_mutation]`, `dep_audit_ran` **False** (not None), `degraded_consumers: []` --
  the `evidence_gone/llm-review BLIND` is gone from the row, so the 0.17.10 fix reads [V] for it.
  **pivot-parlor** no longer stale: 09-24 06:25Z pre-commit under 0.18.0, red on `no_skip_streak` only
  -- and that red is CARRIED: its evidence reads `skip_streaks.pre-push.eslint` = 1 from its newest
  pre-push, 2026-09-13 14:39Z under **0.17.2**; only a pre-push under 0.18.0 re-measures it.
  **pawscout-worker** green -- drop it from this decision.

---

## Workstream API -- make the frozen surface declared, single-sourced and pinned

- [ ] **API-1 Publish the compatibility promise** (BLOCKER, docs, S). New user-guide section + README
  link: the parts from `RELEASING.md:130` plus DEC-7's additions, what "stable within 1.x" means,
  additive-change policy ("an absent JSON key means an older aramid" is today only a code comment,
  `reporter.py:174-176` [A]). *Done when:* `RELEASING.md` points at it instead of enumerating.
- [ ] **API-2 Exhaustive pins for all five parts** (BLOCKER, tests, M). None exists today -- zero hits for
  an exhaustive key-set assertion anywhere in `tests/` [A]. One unit file per part:
  the parser's full subcommand+flag set; the `check --json` top-level key set AND the `models.Finding`
  field set (findings are `dataclasses.asdict`, `reporter.py:180`, so every dataclass field is surface
  [A]); `set(Status)`; the `aramid.toml` key set (`data/defaults.toml` vs the `Config` dataclass vs the
  documented table -- three lists that must agree); the per-subcommand exit-code table.
  *Falsifiability:* each pin goes red when a member is ADDED in a scratch copy.
- [x] **API-3 `schema_version` on every `--json` output** (BLOCKER per DEC-5, M). Additive. Includes giving
  `ledger consumers --json` a fixed envelope (*2026-09-25 [V]: `ledger filter --json`, `resolvers --json`
  and `ledger consumers --json` are all bare LISTS, so all three need an envelope -- a `Changed`
  entry, not additive; `check` and `mutation-score` are objects, additive*) -- today it splats `**e.payload`
  (`commands/ledger_cmd.py:291` [A]), so its shape is whatever a consumer wrote. It shipped in 0.18.0;
  fixing the shape is a `Changed` entry and must land before the freeze.
  *2026-09-25 DONE [V]:* operator ruled "envelope both in 0.19.0" (filter + resolvers as well as
  consumers). Five documents versioned at the PRINT site only -- the MCP tools call these commands
  in text mode (mcp_tools.py `_ledger_filter`/`_resolvers`/`_check` pass no `as_json`), so no MCP
  shape moved. `fleet --json` left as is: the verdict carries its own `schema_version`, and `null`
  (no verdict yet) has no shape to version -- documented. Consumers rows: fixed keys + `extra`
  (the old splat let a payload `at`/`run_id` overwrite the event's). Rendered from src against the
  live ledger: all five lead with `schema_version`. Weak pin fixed: test_ledger_cmd.py's
  `out.strip() != "[]"` would pass on any object -- now `out == ""`.
- [x] **API-4 Version the on-disk stores** (BLOCKER per DEC-5, M). `PRAGMA user_version` on
  `.aramid/ledger.db` with a refuse-newer / migrate-older path (`ledger.py` has neither [V]); version
  fields for `~/.aramid/repos.toml` and `autolearn_state.json` (*2026-09-25 [V]: autolearn_state.json
  ALREADY carries `version` and `autolearn.load_state:60` cold-starts on a foreign one -- only
  ledger.db and repos.toml remain; also make `Ledger.events()` survive an EventType a newer 1.x
  added -- `EventType(t)` raises today*). A 1.x must be able to tell a 2.x
  ledger from a corrupt one.
  *2026-09-25 DONE [V]:* ledger `user_version` 1 (literal PRAGMA, stamp non-fatal when locked, never
  re-stamped on a current ledger), `LedgerTooNew` -> `check` exit 3 [rehearsed]. Unknown event kinds
  are KEPT as `UnknownEventType` (a str with `.value`), not skipped -- autolearn.rollup's cursor is
  `len(events)` (autolearn.py:240-312), so skipping would shift positions between versions.
  repos.toml `schema_version = 1`; newer -> reads empty, never rewritten; `uninstall` exits 3 after
  every other step. Found on the way: drain's "3 when the registry is unusable" (docstring, `--help`,
  user guide, KB) was never true -- an unreadable registry exited 0 as an empty fleet. Fixed to match
  (`load_registry(strict=True)`). Rehearsed on a backup copy of the live ledger: the 0.18.0 wheel
  reads the stamped ledger with identical `ledger filter` output and the versioned registry; the
  live ledger stayed at user_version 0.
- [ ] **API-5 Config validation** (BLOCKER per DEC-4, M). One known-key schema; WARN on unknown table,
  unknown key, key in the wrong table, wrong type; `doctor` row. Reconcile `defaults.toml` with the
  `Config` dataclass (`[shadow]` has a dataclass field and no defaults entry [A]).
- [ ] **API-6 `Status` enum as the single source** (SHOULD, S). `models.py:14-42` defines ten members but
  `ledger.py:477-542` writes all ten as bare literals and never imports it; graphite
  `callers Status` -> 0 [A]. Writer uses the enum, or a pin asserts writer literals == enum.
- [x] **API-7 Exit-code normalisation** (BLOCKER per DEC-6, S). `hooks` refusal -> 3; delete the
  unreachable `return 2` at `hooks_template.py:134` [V]; document codes for `fleet`, `resolvers`,
  `notices`, `mutation-score`, `agent-hook`, `hooks`, and `ledger resolve`'s max-across-ids rule.
  *2026-09-25 DONE [V]:* refusal -> 3. The unknown-action fallthrough is KEPT and returns 3, not
  deleted -- deleting it makes a direct call return None, and 3 is what the CLI already gives for
  that input via argparse `choices`. KB exit table gained the seven rows, each re-read against the
  code; `status`'s exit-0-in-every-branch contract pinned (it killed the three pre-existing
  stage-1 survivors on the `status` lines). `cmd_hooks` sweep: 11 mutants, 0 survivors.
- [ ] **API-8 Inert and legacy keys** (BLOCKER, S). Per DEC-3 bless `test_command`. `[dast].block_armed`
  is flag-shaped, documented RESERVED and never read [A] -- remove it from `defaults.toml` or make it
  warn when set; a user who sets a BLOCK flag that does nothing has been misled.
- [ ] **API-9 Rename the misleading guard** (SHOULD, S). `tests/unit/test_fleet_docs.py:24`
  `test_user_guide_documents_every_surface` asserts six fleet strings [A]; the name tells a maintainer
  the surface is guarded.
- [ ] **API-10 Packaging metadata** (BLOCKER at the 1.0 bump, S). `Development Status :: 3 - Alpha`
  (`pyproject.toml:24` [V]) -> `5 - Production/Stable`; pin it in `test_packaging_metadata.py` against
  the major version so it cannot lag again; DEC-8's cap policy written into `RELEASING.md`.

## Workstream DOC -- documentation truth (docs-only unless marked WHEEL)

Security-relevant first. Every item was found by keyword-led audit; assume more exist (see DOC-11).

- [x] **DOC-1 Hook shim table teaches a tampered shim** (BLOCKER, S). user-guide:96-100 shows
  `"$INTERP" -m aramid ...` [V]; the real shims are `ARAMID_HOOK=<gate> "$INTERP" -P -m aramid ...`
  (`hooks.py:171`, `:293` [V]). The guide itself grades a `-P`-stripped shim as *tampered*, and
  `ARAMID_HOOK` decides whether the gate reads the hook's ref lines.
- [x] **DOC-2 Arming-flag inventory is wrong in four places** (BLOCKER, S). KB:94, :264, :313 and
  user-guide:591 say `[mutation]` has no arming flag [V]; `policy.py:119/132/146` read
  `mutation_block_armed`, `score_block_armed`, `red_proof_block_armed` [V], plus `[shadow]`. KB:439
  documents 2 of `arm`'s 8 flags; user-guide:737 says only `--llm`/`--autolearn` are exclusive (all
  eight are, `cli.py:183-191`); the section-9 arming table lists 5 of 10 flags [A].
- [x] **DOC-3 `--strict`** per DEC-2 (BLOCKER, S): README:93, KB:481 [V].
- [x] **DOC-4 The scheduler is not Windows-only** (BLOCKER, S, part WHEEL). user-guide:551 [V];
  `cli.py:197` help string [V] (WHEEL); README:139-140, :154 [A]; `ARAMID.md` template :177 [A]
  (WHEEL + consumers re-run `init`). Code: `commands/schedule.py` installs a marked crontab line [V].
  *2026-09-25:* docs part DONE (user-guide scheduler section rewritten per platform, README x2, KB:474
  `-P` and KB:543 cron). REMAINING (WHEEL, REL-1): `cli.py:197` help, template :177.
  *2026-09-25 DONE [V]:* both, rendered from source; ARAMID.md regenerated from `src`.
- [ ] **DOC-5 "1.0 waits for that" is already done** (BLOCKER, S, part WHEEL). user-guide:565 says the
  gate does not audit pyproject-only repos [V]; user-guide:118 and `runners/deps.py:199-205` say it
  does (fixed `cabe549`). Same stale premise in the `health.py:299-307` docstring [A] (WHEEL).
  *2026-09-25:* user-guide sentence DONE [V against `health._dep_audit_ran` + `deps.run_python`].
  REMAINING (WHEEL): the docstring. NEW FINDING -> FN-10: a Python-stack repo with NEITHER a
  requirements file NOR a `[project]` table gets `MISSING` (`deps.py:187-189`), so `dep_audit_ran`
  reads False (red) on every pre-push with nothing to audit -- the likely cause of File Convert's.
- [x] **DOC-6 What `doctor` probes** (BLOCKER, S). README:50 and CONTRIBUTING:19 claim eslint (and mypy);
  `doctor.py:69` probes four tools plus pytest/npm [A]. user-guide:150 and KB:192 say doctor does not
  probe pytest/npm; it does (`probe_tests` [V]). Fix the docs now; FN-4 decides whether the product
  should catch up to the README instead.
- [ ] **DOC-7 gitleaks guidance contradicts itself** (SHOULD, S). CONTRIBUTING:20-22 says install it
  yourself and that CI pins 8.28.0; `doctor --fix` installs 8.21.2 (`doctor.py:78`) into
  `~/.aramid/tools`, so README:41 "requires gitleaks on PATH" is also wrong [A]. Pairs with FN-5.
- [x] **DOC-8 `ARAMID.md` template states facts that are false per repo** (BLOCKER, S, WHEEL + `init`
  re-run). :167-171 "a two-week WARN-only bake is in effect" -- nothing enforces two weeks and this
  repo is armed (`aramid.toml:24` [V]); :178 the regression pack is "committed" -- this repo has none
  [A]. The template must describe state conditionally or point at `aramid status`.
  *2026-09-25 DONE [V]:* bake text points at `aramid status` (`_bake_lines` prints `bake in progress,
  day N` only while unarmed); pack text says the file appears once `pack compile`/`add` has a resolved
  finding, and names `pack_block_armed` (default true). Consumers re-run `init` -- announce at release.
- [x] **DOC-9 Version examples** (SHOULD, S). user-guide:34 `aramid 0.1.0`, README:24/:30 `v0.2.0` [V],
  KB:378 [A]. Replace with a form that cannot go stale (`aramid X.Y.Z`), or pin with a test.
- [x] **DOC-10 README roadmap** per DEC-1 (BLOCKER, S). Phase 3 shipped as the agent surfaces
  (`commands/agent_hook.py`, MCP, `arm --agent`); Phase 4 partly shipped (OpenRouter monthly cap,
  spend ledger, regression pack, mutation-score regressions, fleet/notices) [A].
- [ ] **DOC-11 Cover the undocumented surface** (BLOCKER, M). No user-guide coverage at all: `aramid
  hooks`, `aramid mutation-score`, `aramid agent-hook` as a command, `arm --tdd/--mutation/
  --mutation-score/--red-proof/--shadow`, the `[shadow]` runner. Missing from the KB reference:
  `fleet`, `notices`, `resolvers`, `ledger consumers`, the `[tdd]`, `[red_proof]`, `[deps]`,
  `[hooks]`, `[shadow]` tables, top-level `tdd_block_armed`/`agent_block_armed`, and any table of the
  16 `check --json` keys [A].
- [ ] **DOC-12 Drift guard** (BLOCKER, tests, S -- lands with API-2). A unit test that fails when a parser
  subcommand/flag or a `defaults.toml` key has no mention in the reference doc. Without it DOC-1..11
  decay again within a month; this audit is the proof.
- [ ] **DOC-13 `SECURITY.md` supported versions** per DEC-9 (BLOCKER at the 1.0 bump, S).
- [x] **DOC-14 Stale CI comment** (SHOULD, S). `aramid.yml:235-242` calls the windows/py3.14 intermittent
  "unidentified" [V]. Record: 08-06 occurrence fixed 08-08 (strong circumstantial); 09-14 recurrence
  on the same leg and step was NAMED by the dump step and fixed in `bd37772` [V via `gh api` + log].
- [ ] **DOC-15 Known-limitations section** (BLOCKER, S). One user-facing list of what 1.0 knowingly does
  not do -- every ACCEPT item below lands here rather than in code comments.
- [x] **DOC-16 `--strict` help string** (found 2026-09-25 by sweeping every file for the DOC-3 claim,
  WHEEL, S): `cli.py:69` `help="CI mode: treat 2/3 as failure"` [V]. KB:405 had it too (fixed).

*Docs batch 2026-09-25 [V, each cited line re-read first]:* DOC-1, -2 (+ KB fuzz line 361, see FN-2),
-3 (+ KB:405), -6, -9, -10, -14 done; docs halves of DOC-4 and DOC-5 done.

## Workstream FN -- functional gaps

- [ ] **FN-1 Registry management as a command** (BLOCKER, M). `registry.deregister` is reachable only via
  `cmd_uninstall` (graphite `callers deregister`, decision_grade [V]), which strips hooks and exits 3
  on a path that no longer exists (`commands/uninstall.py:29-36` [V]) -- so a vanished repo cannot be
  removed by any CLI, and it pins the verdict at `insufficient-data` forever (`fleet.py:384/419` [A]).
  Add `aramid fleet deregister <path|name>` (timestamped backup of `repos.toml`, works on vanished
  paths) and have the judge report `missing path:` instead of `no rows:`. The registry has been edited
  through a hand-run Python call on at least five occasions (09-03 deregistration, four worktree
  cleanups 09-18/19) [I].
- [x] **FN-2 The fuzz consumer cannot fail visibly** (BLOCKER, M). Driver timeout, driver crash and
  unparseable driver output are all recorded OK; it can never return DEGRADED and has no give-up valve
  (KB:361 [V]). A permanently broken fuzz consumer is indistinguishable from a clean one -- the exact
  failure `consumers_healthy` exists to catch. Give it the DEGRADED + give-up shape the other
  consumers have. Behaviour change (`drain` exit 2, fleet criterion 2): a `Changed` entry.
  **2026-09-25 [V]: PREMISE FALSE -- no code change.** `consumers/fuzz.py:283-295` already returns
  `degraded` on a crashed/non-zero driver and on unparseable output, with a 3-per-head give-up
  (`_DRIVER_GIVE_UP`, :177-180; landed `6a97508` 2026-08-11). The timeout path is `ok` on purpose
  with the `no cases run` marker, which `health._no_work` turns into `consumers_healthy` red. KB:361
  was the stale source; rewritten in the docs batch. Re-graded to DONE (docs).
- [ ] **FN-3 Fuzz budget is spent on functions it cannot fuzz** (SHOULD, S). `consumers/fuzz.py:232`
  charges the `max_functions` budget for AST candidates; hintability is only known in the driver
  (`:303`). Real row: `functions_seen 10, functions_fuzzed 0, skipped_unhinted 10, truncated True`
  [A/I]. Charge on fuzzed, and say `N skipped (unhinted)` in the note.
- [ ] **FN-4 The JS stack is the weakest evidence in the fleet** (BLOCKER to investigate, size unknown).
  Every red JS consumer row is red on something JS-specific: `js_mutation` degraded on pawscout-worker
  and File Convert; eslint skipped on pivot-parlor and File Convert [V]. (a) Root-cause pawscout item
  `c5bf77c1` -- red on five drains, green by hand, cause unknown [I]; 0.18.0 shipped the baseline log
  to catch it; no red row logged since [V]. (b) The two recorded leads (`status` names the queued item
  id; defer-while-gating / retry-once in the drain) stay UNBUILT until a logged red row exists.
  (c) Decide whether `doctor` should probe eslint/tsc when a `package.json` is present -- it would
  have surfaced both skip streaks on day one. (d) Ask the three repos' agents, via the channel,
  whether eslint resolves in their gate environment.
  *2026-09-25 [V]:* pawscout's `js_mutation` is green under 0.18.0 (notice cleared 09-21 10:11Z) with no
  red row logged, so (b) stays unbuilt. eslint now skips under the CURRENT wheel on File Convert
  (streak 5, 0.18.0) and on pivot-parlor (streak 1, carried from a 0.17.2 pre-push) -- two JS repos, one
  tool: read as a candidate aramid defect first (`first-run-shapes-need-a-fresh-repo`), and add File
  Convert's `dep_audit_ran False` to the same investigation. Nothing asked of their agents until
  Phase 0 is ruled.
- [ ] **FN-5 One gitleaks version** (SHOULD, S). `doctor --fix` pins 8.21.2, CI pins 8.28.0 [A] -- the
  DRIFT class the user guide warns about, manufactured in-house. Align them and pin the pair with a
  test that reads both files.
- [ ] **FN-6 Single-suite missing test tool** (SHOULD, decide). A dual-stack repo gets a
  `tests-tool-missing` BLOCK; a single-suite repo whose only test binary is missing degrades silently
  by design (`runners/tests.py:156-184` [V]; KB:192 calls it the more common case). Decide whether 1.0
  keeps that asymmetry; if yes it goes in DOC-15.
- [ ] **FN-7 Silent auto-learn degradation** (SHOULD, S). An unreadable `autolearn_state.json` becomes an
  empty cold start with no notice (KB:129 [A]). Add a `doctor`/`status` line.
- [ ] **FN-8 Tool paths in `check --json`** (SHOULD, decide). Suppression `a3dde015` concedes the
  reviewer's redacted-by-default shape is "a reasonable future shape" [V]. It changes a JSON key's
  content, so: before the freeze, or never in 1.x.
- [ ] **FN-10 `dep_audit_ran` is red for a Python repo with nothing to audit** (found 2026-09-25, SHOULD,
  S, behaviour -> REL-1). See DOC-5's note. Decide: None (n/a) when neither manifest exists.
- [ ] **FN-11 Legacy `test_command` is not honoured by the mutation consumer** (found 2026-09-25 by the
  API-5 key survey [A], BLOCKER per DEC-3, S). `consumers/mutation.py:337` tries `[mutation].test_command`
  then `[tests].command`, never the blessed top-level alias the gate honours (`pipeline.py:971`).
- [ ] **FN-12 Dead config key with a false comment** (found 2026-09-25 [A], S, WHEEL): `[llm].model_openrouter`
  (`defaults.toml:87`) is read nowhere; its comment (:85-86) says the openrouter provider reads it.
  Remove or wire it (API-5/API-8). Also inert: `scope_subpath` (written by `init`, never used).
  *2026-09-25:* `model_openrouter` REMOVED [V: src reads it nowhere; the cap IS read, openrouter.py:22 +
  status.py:243]. `scope_subpath` moves to API-8 -- and it is worse than inert: `init` prints
  `scan scope: <subpath>` (init.py:584) for a scope no runner applies.
- [ ] **FN-13 A fixture date expired and the gate caught it** (FOUND AND FIXED 2026-09-25, `faa8c77`):
  three integration tests pinned a verdict's `computed_at` to 2026-09-20T12:00Z and went red 12 h
  later; `main_push.sh` runs tests/unit only, so only the next full-suite gate saw it. Time-travel
  sweep (scratchpad `tt/timetravel.py`, datetime.now shifted +0/+3/+10/+40 d over the 11 files that
  hold a literal timestamp and reach a real-clock surface) found no other. Worth a CI leg? Decide.
- [ ] **FN-9 ACCEPT and document (DOC-15), no code:** the LLM resolver's "evidence quote gone" proxy
  leaves a genuinely fixed finding open until overridden (`review.py:428-450`); a dead override on the
  three synthesized producers is never reported (`policy.py:293`); `mark-rotated`/`mark-not-a-secret`
  have no inverse (KB:419); cargo and go test suites are not detected (`doctor.py:616`); the
  tests-budget notice cannot tell whether a suite was truncated (`pipeline.py:610`); pnpm/yarn audit
  shape fixtures are hand-authored and unverified (`runners/deps.py:401`); four semgrep rule shapes
  are known-missed (`test_semgrep_rules.py:673`); mutation confirms at unit scope only, so
  integration-only coverage reports survivors (3 standing suppressions); macOS uses cron, not launchd,
  and cron has no run-when-available. [all A]

## Workstream PLAT -- proof off this machine

All field evidence to date is one Windows machine and one operator [V]. CI is 7 legs (ubuntu
3.11-3.14, windows 3.12/3.14, macos 3.12).

- [ ] **PLAT-1 Installed-wheel proof in CI, every OS** (BLOCKER, M). `aramid.yml:170-174` says in its own
  words that nothing loads semgrep against a non-editable install and a green run is not wheel-level
  proof; the sdist is built and smoke-tested only at release time [A]. Add to the matrix: build wheel
  + sdist, install into a clean venv, run a real `check` with semgrep against a fixture. This class
  has already cost one failed tag run (0.17.10, `tomli_w` via the sdist smoke [I]).
- [ ] **PLAT-2 POSIX field rehearsal** (BLOCKER, M). On a real Linux and a real macOS (throwaway workflow
  on a rehearsal branch, or a VM): `pip install aramid` from PyPI -> `aramid init` -> commit and push
  through the real hooks -> `aramid schedule install/status/remove` against a real crontab -> one
  drain fired BY cron, recording `os.getcwd()`. `schedule.py:141-143` says the cron cwd is "NOT
  measured here" [V], and the `-P` rationale on that path rests on it.
- [x] **PLAT-3 Does the real-npm JS mutation test run in CI?** (BLOCKER to measure, S).
  `tests/integration/test_js_mutation_consumer.py:303` skips without node+npm, reason "(Python-only
  CI)" [V]. GitHub-hosted runners usually ship node, so it may well run -- UNMEASURED. Add `-rs` to the
  CI pytest step (or assert node presence) and read one run. If it is skipped everywhere, the live
  path of FN-4's consumer has never run in CI on any OS.
  *2026-09-25:* the 93fec9b legs report 15 skipped (ubuntu, macos) / 5 (windows), UNNAMED -- not
  measurable from existing logs [V]. `-rs` added to the full-suite step; read the next run, and
  count that the named skips add up to the leg's skip total before reading absence as "it ran".
  *2026-09-25 MEASURED [V, CI run 36107735629 on b85d45f, attempt 1, 7/7 green]:* YES, on all three
  OSes. ubuntu/py3.12 and macos/py3.12: 15 named skips = 1+5+2+1+3+1+1+1, the leg total; windows/
  py3.12: 5 = 2+1+1+1, the leg total. `test_js_mutation_consumer.py:303` is in none of the three lists,
  so it RAN everywhere. The skips: 11 Windows-only paths off Windows (schedule e2e/unit, windows_hooks
  x5, hooks drive-letter x3, toolpath exe suffix) and `test_schedule.py:46` (the off-Windows install
  path) on Windows; `test_arm_misplaced_key.py:95` x2 (parametrize case with nothing to test);
  `test_checkout_env.py:58` (vacuous under CI's editable install -- by design, its control says so);
  `test_triage.py:120` (no graphite graph in a CI checkout -- runs locally only).
- [ ] **PLAT-4 Stranger onboarding, twice, per OS** (BLOCKER, M). Clean machine, no gitleaks, README
  only: install, `doctor --fix`, `init`, first commit, first push, `schedule install`. Run it twice;
  a control you did not watch fire is not a control.
- [ ] **PLAT-5 Upgrade rehearsal 0.x -> 1.0** (BLOCKER, S). A copy of a real 0.x ledger + `aramid.toml`
  under the 1.0 candidate: no re-fingerprinting, no lost overrides/suppressions, or a documented
  `rebaseline`. Becomes the "Upgrading to 1.0" section.
- [ ] **PLAT-6 Contention sweep of the suite** (SHOULD, M). Both CI flakes since August were wall-clock
  assertions failing at the gate step, where the unit suite runs alongside four analyzers [I/V]. Run
  `tests/unit` under 2x CPU oversubscription N times (prove the load with an independent observable),
  fix what trips. `xfail_strict = true` so a fixed semgrep gap cannot pass silently [A].

## Workstream FLEET -- the readiness verdict (1.0 gate, item 1)

aramid's own row is green on all five criteria [V]. Everything red belongs to another repo; the work
here is decisions, channel recommendations and waiting.

- [ ] **FLEET-1** DEC-10 executed (needs FN-1 if any repo is deregistered).
- [ ] **FLEET-2 Channel recommendations** (re-list first; their agents decide): pawscout-worker -- a gate
  run under 0.18.0, and on the next red `js_mutation` row post
  `.aramid/logs/js-mutation-baseline-*.log`; File Convert -- a pre-push under >= 0.17.10 clears
  `evidence_gone/llm-review BLIND`, then eslint + `js_mutation`; pivot-parlor -- eslint availability +
  a fresh gate; Atlas_Data -- turn a mutation knob (`[mutation].wall_budget_s` /
  `[mutation].test_command`) or leave the fleet.
  *Per the DEC-10 ruling:* the three removed repos get an informational round when FN-1 executes (they
  leave the readiness count; their gates keep running; nothing asked). The eslint/dep-audit question
  stays an aramid investigation under FN-4, not a revival request.
- [ ] **FLEET-3 Streak watch.** From the first all-green judgement: 14 days, >= 2 versions, no disarm, no
  repo silent > 7 days. Earliest possible `ready` = that date + 14 d. Notice `2a0c051b89e1` clears
  itself; do not ack.
- [ ] **FLEET-4** Land the two clean releases (REL-2, REL-3) INSIDE the streak so one fortnight satisfies
  both the version count and the API freeze.

## Workstream REL -- sequencing to the tag

Each release on the operator's word; tag push plain; approval by the operator's `!` line; digests x3;
promote; verify x2; rehearsal tag; channel round.

- [ ] **REL-1 0.19.0 -- the surface release.** Everything that is a `Changed`/`Removed`/behavioural entry:
  API-3, API-4, API-5, API-7, API-8, FN-1, FN-2, FN-3, FN-8 (if taken), DOC-4/5/8 wheel parts. Last
  release allowed to touch the surface. Consumers re-run `aramid init` (template).
- [ ] **REL-2 0.19.1 -- first clean release.** Pins (API-2, API-6, API-9, DOC-12), PLAT-1, docs. No
  `Changed`/`Removed` against the surface.
- [ ] **REL-3 0.19.2 -- second clean release.** PLAT-2..6 fallout, FN-4 outcome, DOC-15. No
  `Changed`/`Removed`. After it, criterion 7 holds again.
- [ ] **REL-4 1.0.0.** Preconditions: `aramid fleet` reads `ready`; REL-2 and REL-3 clean; PLAT-2/4/5
  rehearsed; every BLOCKER above ticked. The bump carries API-10, DOC-13, the README status line and
  a CHANGELOG entry that states the compatibility promise. Cutting on a `not-ready` verdict is a
  decision to write in the changelog, never a silent one (`RELEASING.md:132`).

## Order of work

1. Phase 0 decisions (one sitting; DEC-1, -2, -6, -10 unblock the most).
2. In parallel, no release needed: DOC-1, -2, -3, -6, -9, -10, -14 (one docs commit), PLAT-3 (measure),
   FLEET-2 (channel).
3. REL-1 content, one plan per group: {API-3, API-4} versioning; {API-5, API-8} config; {API-7}
   exit codes; {FN-1} registry; {FN-2, FN-3} fuzz.
4. REL-2 content: {API-2, DOC-12} pins + drift guard; PLAT-1.
5. PLAT-2, -4, -5 rehearsals against the REL-2 wheel; REL-3.
6. Wait out FLEET-3; REL-4.

**Not measured by this audit:** the suite was not run; coverage was not read; agent-reported [A] items
were spot-checked, not all re-read; no other repo was opened (their state is known only through
aramid's own `fleet_health.jsonl`); whether GitHub PVR is still enabled was not re-checked.
